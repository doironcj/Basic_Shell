This a basic shell program written in c for an operating systems course.  The shell program includes all the basic built in commands of a regular shell including exit, jobs, help, cd, history, and kill. The help command gives a description of the various built in commands.  All commands can be excecuted in the background with '&' typed after the command and arguments.  All input and output can be redirected with '<' and '>'.